#include <stdio.h>
 int main()
    {
        int a, b;
        printf("Enter two number");
        scanf("%d\n%d",&a,&b);
        printf("sum of %d and %d is %d ",a,b,a+b);
        return 0;

    }