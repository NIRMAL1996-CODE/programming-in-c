#include<stdio.h>
     main()
    {
      int a ; 
      printf("Enter a number");
      scanf("%d",&a);
      if(a>=0)
      {
        printf("positive");
      }
      else
      {
        printf("negative");
      }
      return 0;

    }
