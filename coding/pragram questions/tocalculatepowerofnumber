#include<stdio.h>
#include<math.h>
int main()
{
    int x , y ;
    printf("enter a number to find a power of \n");
    scanf("%d",&x);
    printf("enter a power\n");
    scanf("%d",&y);
    
    printf("power = %.1f", pow(x,y));
    return 0;
}